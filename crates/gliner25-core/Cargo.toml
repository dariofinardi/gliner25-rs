[package]
name = "gliner25-core"
version = "0.1.0"
description = "GLiNER2.5 boundary-architecture inference engine on ONNX Runtime: prompt construction, candidate decoding, overlap policies"
readme = "README.md"
keywords = ["nlp", "ner", "onnx", "inference", "gliner"]
categories = ["science", "text-processing"]
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
ort.workspace = true
ndarray.workspace = true
tokenizers.workspace = true
anyhow.workspace = true
half.workspace = true
regex.workspace = true
serde.workspace = true
serde_json.workspace = true

[features]
# Helpers that locate a tokenizer.json under the workspace, so downstream crates
# can pin the prompt layout against the same ground truth.
test-support = []
