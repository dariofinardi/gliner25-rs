[package]
name = "gliner-core"
version = "0.1.0"
description = "Shared foundation for the GLiNER Rust engines: prompt construction, ONNX Runtime helpers, overlap policies"
readme = "README.md"
keywords = ["nlp", "ner", "onnx", "gliner"]
categories = ["science", "text-processing"]
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
ort.workspace = true
ndarray.workspace = true
tokenizers.workspace = true
anyhow.workspace = true
half.workspace = true
regex.workspace = true

[features]
# Test helpers that locate a tokenizer.json under the workspace, so downstream
# crates can pin the prompt layout against the same ground truth.
test-support = []
